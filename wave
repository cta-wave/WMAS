#!/bin/bash

if [ "$1" == "init" ]; then
  python -m venv venv &&\
  source venv/bin/activate &&\
  pip install -r requirements.txt
elif [ "$1" == "start" ]; then
  source ./venv/bin/activate &&\
  python ./wpt serve
fi
